{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "hatch",
            "type": "shell",
            "command": "hatch config set dirs.env.virtual .hatch; hatch config update; hatch shell",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}